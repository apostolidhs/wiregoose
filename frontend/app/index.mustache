<!DOCTYPE html>
<html ng-app="wg.app" lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <title>{{config.NAME}}</title>

    {{#thirdPartyCss}}
      <link rel="stylesheet" href="{{#stripBase}}{{{.}}}{{/stripBase}}"/>
    {{/thirdPartyCss}}
    <link rel="stylesheet" href="{{{appCss}}}"/>

    <script>
      (function(global){
        global.wg = global.wg || {};

        var config = {};
        config.APP_URL = '{{{config.APP_URL}}}/{{{config.API_URL_PREFIX}}}';
        config.NAME = '{{config.NAME}}';
        config.SUPPORTED_LANGUAGES = '{{config.SUPPORTED_LANGUAGES}}';

        global.wg.config = config;
      })(window);
    </script>

    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>


<body ng-controller="AppController as appCtrl">
  <div ui-view>
  </div>


<!--   <body ng-controller="AppController as appCtrl">
    <h1>Hello, world!</h1>

    <wg-crud-all ng-if="appCtrl.session" crud-model="appCtrl.crudModel"></wg-crud-all>-->

    {{#thirdPartyScripts}}
      <script src="{{#stripBase}}{{{.}}}{{/stripBase}}"></script>
    {{/thirdPartyScripts}}
    <script src="{{{appScript}}}"></script>

  </body>
</html>

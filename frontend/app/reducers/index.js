const session = (
  state = {
    user: undefined,
    isRequesting: false
  },
  action,
) => {
  switch (action.type) {
    case 'SESSION_'
  }
};

const wiregooseApp = combineReducers({
  session
});

export default wiregooseApp;